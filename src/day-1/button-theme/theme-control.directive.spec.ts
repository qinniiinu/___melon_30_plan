import { ThemeControlDirective } from './theme-control.directive';

describe('ThemeControlDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemeControlDirective();
    expect(directive).toBeTruthy();
  });
});
