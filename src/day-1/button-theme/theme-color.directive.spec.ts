import { ThemeColorDirective } from './theme-color.directive';

describe('ThemeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemeColorDirective();
    expect(directive).toBeTruthy();
  });
});
